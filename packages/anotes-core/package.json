{
  "name": "@anotes/core",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "dependencies": {
    "@anotes/parser": "*",
    "katex": "^0.16.22",
    "marked": "^16.1.2"
  },
  "scripts": {
    "compile": "rm -fr dist; tsc",
    "pretest": "npm run compile",
    "test": "node --test 'dist/**/*.test.js'"
  }
}
