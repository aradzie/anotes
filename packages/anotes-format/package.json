{
  "name": "@anotes/format",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "dependencies": {
    "katex": "^0.16.22",
    "marked": "^16.2.0"
  },
  "scripts": {
    "compile": "rm -fr dist; tsc",
    "pretest": "npm run compile",
    "test": "node --test 'dist/**/*.test.js'"
  }
}
