{
  "name": "@anotes/cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "anotes": "anotes.js"
  },
  "dependencies": {
    "@anotes/core": "*",
    "commander": "^14.0.0"
  },
  "scripts": {
    "compile": "rm -fr dist && tsc",
    "pretest": "npm run compile",
    "test": "node --test 'dist/**/*.test.js'"
  }
}
