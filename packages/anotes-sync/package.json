{
  "name": "@anotes/sync",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "dependencies": {
    "@anotes/core": "*",
    "@anotes/format": "*",
    "@anotes/parser": "*"
  },
  "scripts": {
    "compile": "rm -fr dist && tsc",
    "pretest": "npm run compile",
    "test": "node --test 'dist/**/*.test.js'"
  }
}
